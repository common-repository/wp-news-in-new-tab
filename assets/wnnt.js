/* 
 * Add Wordpress news new Tab.
 */
jQuery(document).ready(function ($){
    jQuery.noConflict();
    var anchors = $('#dashboard_primary').find('div.inside a.rsswidget').attr('target','_blank');
});